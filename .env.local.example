# Upstash Redis
# Get these from: https://console.upstash.com/ → Create Redis Database
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Admin API Key (for retrieving applications via GET /api/lead)
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=
